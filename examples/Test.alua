function load(feedId: FeedId): Feed
  database.withTransaction do connection =>
    protect do
      loadFeedById(feedId)
    end
  end
end


function bar(
  x : Option[String] = None,
  xs : List[String] = ...
) : Int
  if c then
    1
  elseif d then
    xs.fold(0, x; y => x)
    f(=> x)
    g(x => x)
  else
    Foo(1, 2).foo(1, 2)()
  end
end


function twice(x)
  add(x, x)
end

